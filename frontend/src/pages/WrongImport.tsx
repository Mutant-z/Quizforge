import WrongImportWorkspace from '@/components/wrong-import/WrongImportWorkspace'

export default function WrongImport() {
  return (
    <div className="h-[calc(100vh-4.5rem)] min-h-[620px] w-full overflow-hidden rounded-2xl border border-border/70 bg-surface shadow-card transition-all">
      <WrongImportWorkspace />
    </div>
  )
}
